{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}\"/scripts/session-start.sh"
          }
        ]
      },
      {
        "matcher": "compact",
        "hooks": [
          {
            "type": "command",
            "command": "echo '## Session Context Restored' && echo '' && echo 'Knowledge base: .claude/knowledge/index.json' && echo 'Rules: ${CLAUDE_PLUGIN_ROOT}/rules/ (project overrides: .claude/rules/)' && echo '' && echo 'Run `ah knowledge search <query>` to find relevant context.'"
          }
        ]
      }
    ]
  }
}
